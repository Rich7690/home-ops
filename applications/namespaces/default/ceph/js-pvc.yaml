apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: js-config
  namespace: default
spec:
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
