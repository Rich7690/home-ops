apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbit-config
  namespace: default
spec:
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
