apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vw-config
  namespace: default
  annotations:
    reclaimspace.csiaddons.openshift.io/schedule: "1 0 * * 6"
    backup_schedule: "1 1 * * 6"
    home.backup.io: "true"
spec:
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
