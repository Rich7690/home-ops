apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-config
  namespace: default
spec:
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
