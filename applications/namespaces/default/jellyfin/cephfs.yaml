apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: default
spec:
  storageClassName: rook-ceph-storagefs
  resources:
    requests:
      storage: 8Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
