apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jelly-config
  namespace: default
spec:
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 20Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
