apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: qbit
  namespace: default
spec:
  endpointSelector:
    matchExpressions:
      - key: app.kubernetes.io/name
        operator: In
        values:
          - qbittorrent
  ingress:
    - fromEndpoints:
        - matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
                - radarr
                - sonarr
                - ingress-nginx
  egress:
    - toEntities:
       - cluster
    - toCIDRSet:
        - cidr: 199.229.250.59/32
      toPorts:
        - ports:
            - port: "51820"
              protocol: UDP
