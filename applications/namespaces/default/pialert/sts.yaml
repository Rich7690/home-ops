apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pialert
spec:
  selector:
    matchLabels:
      app: pialert
  serviceName: pialert
  replicas: 1
  podManagementPolicy: Parallel
  revisionHistoryLimit: 3
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: pialert
    spec:
      hostNetwork: true
      containers:
      - name: pialert
        image: jokobsk/pi.alert:24.6.8@sha256:c6d7008e6f5dc916d37f9b544250a286be3358f7515f607ff4fc5718c111d7f5
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          seccompProfile:
            type: RuntimeDefault
        env:
          - name: TZ
            value: America/Los_Angeles
          - name: PORT
            value: "20211"
        ports:
        - containerPort: 20211
          name: http
        volumeMounts:
        - name: pialert
          subPath: config
          mountPath: /home/pi/pialert/config
        - name: pialert
          subPath: db
          mountPath: /home/pi/pialert/db
  volumeClaimTemplates:
  - metadata:
      name: pialert
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 10Gi
