apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: js-config-zfs
  namespace: default
spec:
  storageClassName: openebs-zfspv
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
