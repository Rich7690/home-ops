apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vw-config-zfs
  namespace: default
spec:
  storageClassName: openebs-zfspv
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
