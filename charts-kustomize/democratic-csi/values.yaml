controller:
  nodeSelector:
    kubernetes.io/hostname: worker-one
  tolerations:
    - effect: NoSchedule
      operator: Exists
node:
  nodeSelector:
    kubernetes.io/hostname: worker-one
  tolerations:
    - effect: NoSchedule
      operator: Exists
csiDriver:
  # should be globally unique for a given cluster
  name: democratic-csi
