image:  # @schema additionalProperties: false
  # -- Traefik image host registry
  registry: docker.io
  # -- Traefik image repository
  repository: traefik
  # -- defaults to appVersion. It's used for version checking, even prefixed with experimental- or latest-.
  # When a digest is required, `versionOverride` can be used to set the version.
  tag: "v3.6.4@sha256:c5bd185c41ba3dbb42cf8a1b9fbdc368bdc96f90c8e598134879935f64e7a7f1" # @schema type:[string, null]
  # -- Traefik image pull policy
  pullPolicy: IfNotPresent

deployment:
  enabled: true
  replicas: 1

ingressClass:
  enabled: true
  isDefaultClass: false
  name: traefik-internal

service:
  enabled: true
  type: LoadBalancer
  spec:
    loadBalancerIP: "192.168.5.10"
    externalTrafficPolicy: Local
  annotations:
    kube-vip.io/loadbalancerIPs: 192.168.5.10

ports:
  web:
    port: 80
    expose:
      default: true
    exposedPort: 80
  websecure:
    port: 443
    expose:
      default: true
    exposedPort: 443

ingressRoute:
  dashboard:
    enabled: true

providers:
  kubernetesCRD:
    enabled: true
  kubernetesIngress:
    enabled: true
    allowExternalNameServices: true
    ingressClass: traefik-internal
  kubernetesIngressNginx:
    enabled: false
    ingressClass: nginx
logs:
  general:
    level: INFO
  access:
    enabled: true
global:
  checkNewVersion: false
  sendAnonymousUsage: false

nodeSelector:
  "kubernetes.io/hostname": "worker-one"
