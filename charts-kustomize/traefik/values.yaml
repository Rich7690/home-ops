image:  # @schema additionalProperties: false
  # -- Traefik image host registry
  registry: docker.io
  # -- Traefik image repository
  repository: traefik
  # -- defaults to appVersion. It's used for version checking, even prefixed with experimental- or latest-.
  # When a digest is required, `versionOverride` can be used to set the version.
  tag: "v3.6.6@sha256:82d3d16dde0474a51fef00b28de143d48b67f7a27453224d5e7b5aaefff26a97" # @schema type:[string, null]
  # -- Traefik image pull policy
  pullPolicy: IfNotPresent

deployment:
  enabled: true
  replicas: 1

ingressClass:
  enabled: true
  isDefaultClass: false
  name: traefik-internal

service:
  enabled: true
  type: LoadBalancer
  spec:
    loadBalancerIP: "192.168.5.10"
    externalTrafficPolicy: Local
  annotations:
    kube-vip.io/loadbalancerIPs: 192.168.5.10

ports:
  web:
    port: 80
    expose:
      default: true
    exposedPort: 80
  websecure:
    port: 443
    expose:
      default: true
    exposedPort: 443

ingressRoute:
  dashboard:
    enabled: true

providers:
  kubernetesCRD:
    enabled: true
  kubernetesIngress:
    enabled: true
    allowExternalNameServices: true
    ingressClass: traefik-internal
  kubernetesIngressNginx:
    enabled: false
    ingressClass: nginx
logs:
  general:
    level: INFO
  access:
    enabled: true
global:
  checkNewVersion: false
  sendAnonymousUsage: false

nodeSelector:
  "kubernetes.io/hostname": "worker-one"
