apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: zfs-tools
  name: zfs-tools
  namespace: default
spec:
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: zfs-tools
  template:
    metadata:
      labels:
        app: zfs-tools
    spec:
      restartPolicy: Always
      volumes:
        - name: zfs
          hostPath:
            path: /dev/zfs
            type: ""
      nodeSelector:
        kubernetes.io/hostname: worker-one
      tolerations:
        - operator: Exists
      terminationGracePeriodSeconds: 5
      hostNetwork: true
      hostPID: true
      securityContext:
        runAsGroup: 0
        runAsNonRoot: false
        runAsUser: 0
      containers:
        - name: test
          image: ghcr.io/rich7690/home-ops/zfs-utils:latest@sha256:5788cd084813276da6df824f02b4c1dd783203c92b03211275f3d0ce9c8a2b3d
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - mountPath: /dev/zfs
              name: zfs
          securityContext:
            privileged: true
          command:
            - "bash"
            - "-c"
            - "tail -f /dev/null"
  updateStrategy:
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 0
    type: RollingUpdate
