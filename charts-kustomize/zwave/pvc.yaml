apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zwave2
spec:
  resources:
    requests:
      storage: 10Gi
  storageClassName: openebs-zfspv-nvme-generic
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
