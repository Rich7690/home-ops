apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: charts
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/Rich7690/home-server-config.git
      revision: HEAD
      directories:
      - path: charts/default/*
      - path: charts/metrics/*
      - path: charts/kube-system/*
      - path: charts/kyverno/*
      - path: charts/cert-manager/*
  template:
    metadata:
      name: '{{path.basename}}'
      namespace: argocd
      annotations:
        # resolves to the directory
        argocd.argoproj.io/manifest-generate-paths: .
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: remote-apps
      ignoreDifferences:
      - group: '*'
        kind: Secret
        namespace: kube-system
        jqPathExpressions:
          - .data
      source:
        repoURL: https://github.com/Rich7690/home-server-config.git
        targetRevision: HEAD
        path: '{{path}}'
        helm:
          releaseName: '{{path.basename}}'
          valueFiles:
            - values.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path[1]}}'
