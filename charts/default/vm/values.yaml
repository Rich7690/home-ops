victoria-metrics-single:
  rbac:
    pspEnabled: false
  server:
    podAnnotations:
      kyverno-skip: "true"
    scrape:
      enabled: true
      configMap: "metrics-scrapeconfig"
    persistentVolume:
      existingClaim: vm
