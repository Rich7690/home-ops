---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: metallb
    namespace: argocd
spec:
    project: remote-apps-u59
    source:
        chart: metallb
        repoURL: https://metallb.github.io/metallb
        targetRevision: 0.13.4
        helm:
            releaseName: metallb
            values: |

    destination:
        server: https://192.168.5.4:6443
        namespace: default
---
# ## TODO: this only works once the helm chart runs. Fix
# apiVersion: metallb.io/v1beta1
# kind: IPAddressPool
# metadata:
#   name: production
#   namespace: default
# spec:
#   addresses:
#   - 192.168.5.40-192.168.5.49
# ---
# apiVersion: metallb.io/v1beta1
# kind: L2Advertisement
# metadata:
#   name: example
#   namespace: default
