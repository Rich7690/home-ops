apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoami
  namespace: default
  labels:
    name: whoami
spec:
  defaultBackend:
    service:
      name: whoami
      port:
        name: http
