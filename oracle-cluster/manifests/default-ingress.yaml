apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoami
  namespace: default
  labels:
    name: whoami
spec:
  ingressClassName: nginx
  rules:
    - host: whoami.local
      http:
        paths:
          - backend:
              service:
                name: whoami
            pathType: Prefix
            path: /
